Put reusable SVG designs in this folder.

The Interior workspace scans `src/assets/svg-library/**/*.svg` at build/dev time and shows them in the SVG library panel.

After adding or removing SVG files, restart the dev server if the list does not update automatically.
